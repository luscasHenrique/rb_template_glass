<% content_for :title, "Gestão de Termos de Uso" %>

<div class="glass-card">
  <!-- Cabeçalho da Seção -->
  <div class="flex flex-col sm:flex-row justify-between items-center mb-8 gap-4">
    <h2 class="glass-section-title mb-0">
      <%= icon "document-text", class: "glass-icon-primary" %>
      Gestão de Termos (LGPD)
    </h2>
    
    <!-- Botão Modular (Primary) -->
    <%= render "shared/components/button", 
               text: "Novo Termo", 
               type: "link", 
               path: new_policy_term_path, 
               variant: "primary",
               icon: "plus" %>
  </div>

  <!-- Tabela de Dados -->
  <div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-white/10 text-left">
      <thead>
        <tr>
          <th scope="col" class="py-3 px-4 text-sm font-semibold text-white/90">Versão</th>
          <th scope="col" class="py-3 px-4 text-sm font-semibold text-white/90">Resumo do Conteúdo</th>
          <th scope="col" class="py-3 px-4 text-sm font-semibold text-white/90 text-center">Status</th>
          <th scope="col" class="py-3 px-4 text-sm font-semibold text-white/90 text-right">Ações</th>
        </tr>
      </thead>
      
      <tbody class="divide-y divide-white/10">
        <% if @policy_terms.any? %>
          <% @policy_terms.each do |policy_term| %>
            <tr class="hover:bg-white/5 transition-colors duration-200">
              
              <td class="whitespace-nowrap py-4 px-4 text-sm font-medium text-white">
                <%= policy_term.document_version %>
              </td>
              
              <td class="py-4 px-4 text-sm text-white/70 max-w-xs truncate">
                <%= policy_term.content %>
              </td>
              
              <td class="whitespace-nowrap py-4 px-4 text-sm text-center">
                <% if policy_term.active? %>
                  <span class="glass-btn glass-btn-xsm glass-btn-outline-success cursor-default">
                    Ativo
                  </span>
                <% else %>
                  <span class="glass-btn glass-btn-xsm glass-btn-outline-danger cursor-default">
                    Inativo
                  </span>
                <% end %>
              </td>
              
              <!-- Coluna: Ações (Consumindo o Icon Button) -->
              <td class="whitespace-nowrap py-4 px-4 text-right">
                <div class="flex items-center justify-end gap-2">
                  <%= render "shared/components/icon_button", 
                             icon: "eye", 
                             path: policy_term_path(policy_term), 
                             title: "Detalhes", 
                             variant: "info" %>
                  
                  <% if policy(policy_term).edit? %>
                    <%= render "shared/components/icon_button", 
                               icon: "pencil-square", 
                               path: edit_policy_term_path(policy_term), 
                               title: "Editar", 
                               variant: "warning" %>
                  <% else %>
                    <!-- Cadeado com as mesmas dimensões do Icon Button (w-8 h-8) -->
                    <span title="Contrato já possui assinaturas e está imutável" class="inline-flex items-center justify-center w-8 h-8 rounded-xl border border-white/10 text-white/30 cursor-not-allowed bg-black/20">
                      <%= icon "lock-closed", class: "w-4 h-4" %>
                    </span>
                  <% end %>
                </div>
              </td>
              
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="4" class="py-10 text-center text-sm text-white/50 bg-black/20 rounded-xl border border-white/5">
              Nenhum termo de política cadastrado ainda. Clique em "Novo Termo" para começar.
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>