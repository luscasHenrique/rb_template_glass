<% content_for :title, "Nova Integração" %>

<div class="glass-card">
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-8 border-b border-white/10 pb-6">
    <div>
      <h1 class="text-3xl font-bold text-white tracking-tight flex items-center gap-3">
        <%= icon "code-bracket-square", class: "text-emerald-400 w-8 h-8" %>
        Adicionar Integração
      </h1>
      <p class="text-white/60 text-sm mt-1">Configure um novo script de marketing ou rastreio no sistema.</p>
    </div>
  </div>

  <%= render "form", tracking_integration: @tracking_integration %>
</div>