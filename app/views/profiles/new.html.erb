<% content_for :title, "Novo Perfil" %>

<div class="glass-card">
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-8 border-b border-white/10 pb-6">
    <div>
      <h1 class="text-3xl font-bold text-white tracking-tight flex items-center gap-3">
        <%= icon "user-plus", class: "text-emerald-400 w-8 h-8" %>
        Criar Perfil
      </h1>
      <p class="text-white/60 text-sm mt-1">Preencha as informações pessoais do novo usuário.</p>
    </div>
  </div>

  <%= render "form", profile: @profile %>
</div>